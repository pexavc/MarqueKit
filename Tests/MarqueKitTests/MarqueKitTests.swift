import XCTest
@testable import MarqueKit

final class MarqueKitTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(MarqueKit().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
